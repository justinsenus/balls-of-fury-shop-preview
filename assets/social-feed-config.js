/*
 * Leave blank until the account owner creates the feed widget and shares its
 * installation code. Copy only the ID from `elfsight-app-<ID>` into this value.
 */
window.BOF_SOCIAL_FEED_WIDGET_ID = "";
